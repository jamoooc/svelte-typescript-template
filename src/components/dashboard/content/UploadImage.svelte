<script> 

  // import Form from '../../form/Form.svelte';
  // import onSubmit from '../../../utils/onSubmit.js'
  // import Loading from '../../Loading.svelte';
  // import { uploadProfileFields } from './form_fields/uploadProfileFields.js';
  // import schema from './validators/uploadProfileValidator.js';
  // import { loading } from '../../stores.js';

  // let path = `${__myapp.env.UPLOAD_PROFILE}`;
  // let method = 'POST';

</script>

<h3>Upload profile photo</h3>

<!-- {#if $loading}
  <Loading />
{:else}
  <Form
    {onSubmit}
    fields={ uploadProfileFields }
    options={ [ ] }
    optGroups={ [ [] ] }
    validator={schema}
    {path}
    {method}
  />
{/if} -->
