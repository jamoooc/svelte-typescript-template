<script> 

  // import Form from '../../form/Form.svelte';
  // import onSubmit from '../../../utils/onSubmit.js'
  // import Loading from '../../Loading.svelte';
  // import { uploadImageFields } from './form_fields/uploadImageFields.js';
  // import schema from './validators/uploadImageValidator.js';
  // import { loading } from '../../stores.js';

  // let path = `${__myapp.env.UPLOAD_IMAGE}`;
  // let method = 'POST';

</script>

<h3>Upload profile photo</h3>

<!-- {#if $loading}
  <Loading />
{:else}
  <Form
    {onSubmit}
    fields={ uploadImageFields }
    options={ [ ] }
    optGroups={ [ [] ] }
    validator={schema}
    {path}
    {method}
  />
{/if} -->
